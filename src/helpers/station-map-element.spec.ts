import { StationMapElement } from './station-map-element';

describe('StationMapElement', () => {
  it('should create an instance', () => {
    expect(new StationMapElement()).toBeTruthy();
  });
});
