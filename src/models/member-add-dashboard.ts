/** Interface to use to add user to dashboard. */
export interface MemberAddDashboard {
  /** User rithm Id. */
  userRithmId: string;
  /** Is Editable. */
  isEditable: true;
  /** Can view dashboard. */
  canView: true;
}
