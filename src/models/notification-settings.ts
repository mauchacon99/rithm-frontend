/**
 * Represents all information about a notification settings.
 */

export interface NotificationSettings {
  /** The comments settings. */
  comments: boolean;

  /** The comment mentions settings. */
  commentMentions: boolean;
}
