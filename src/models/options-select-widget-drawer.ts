/**
 * Represents data of values mat-select.
 */
export interface OptionsSelectWidgetDrawer {
  /** If disabled option in select. */
  disabled: boolean;
  /** Name option. */
  name: string;
  /** Value option. */
  value: string;
  /** QuestionId option. */
  questionId?: string;
}
