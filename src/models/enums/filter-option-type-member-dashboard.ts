/** Filter options. */
export enum FilterOptionTypeMemberDashboard {
  /** All users. */
  All = 'All',
  /** Filter by users who have read permission. */
  ViewOnly = 'View Only',
  /** Filter by users who can edit. */
  CanEdit = 'Can Edit',
}
