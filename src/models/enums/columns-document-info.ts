/**
 * Represent columns of the list documents in station widget.
 */
export enum ColumnsDocumentInfo {
  /** Document name. */
  Name = 'name',
  /** Priority. */
  Priority = 'priority',
  /** Time in station. */
  TimeInStation = 'flowedTimeUTC',
  /** Last updated. */
  LastUpdated = 'lastUpdatedUTC',
  /** Assigned user. */
  AssignedUser = 'assignedUser',
}
