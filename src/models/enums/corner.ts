/**
 * Represent all possible corner of the station group.
 */
export enum Corner {
  /** The corner top-left. */
  TopLeft = 'topLeft',
  /** The corner top-right. */
  TopRight = 'topRight',
  /** The corner bottom-left. */
  BottomLeft = 'bottomLeft',
  /**The corner bottom-right. */
  BottomRight = 'bottomRight',
}
