/**
 * All status in the documents.
 */
 export enum DocumentGenerationStatus {

  /** Not status. */
  None = 'None',

  /** Status manual. */
  Manual = 'Manual',

  /** Status auto. */
  Auto = 'Auto',

  /** Status both. */
  Both = 'Both'
}
