export enum FieldType {
  SHORT_TEXT = 'string',
  LONG_TEXT = 'text',
  URL = 'url',
  EMAIL = 'email',
  ADDRESS = 'address',
  NUMBER = 'integer',
  PHONE = 'phone',
  CURRENCY = 'currency',
  SELECT = 'select',
  MULTI_SELECT = 'multi-select',
  DATE = 'date',
  DATETIME = 'datetime',
  CHECKBOX = 'checkbox',
  CHECKLIST = 'checklist'
}
