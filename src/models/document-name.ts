/**
 * Represents all information about a document name.
 */
export interface DocumentName {
  /** The base name for document. */
  baseName: string;
  /** The appendedName for Document. */
  appendedName: string;
}
