/**
 * Represents the columns to display in the station-widget.
 */

export interface ColumnFieldsWidget {
  /** Id the question. */
  questionId?: string;

  /** Name the question or field. */
  name: string;
}
